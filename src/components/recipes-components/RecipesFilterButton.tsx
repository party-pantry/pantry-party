import { Button } from 'react-bootstrap';

const RecipesFilterButton: React.FC = () => {
    return (
        <Button variant="primary">Filter Recipes</Button>
    )
}

export default RecipesFilterButton;